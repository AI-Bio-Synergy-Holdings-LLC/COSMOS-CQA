# feeds

Parsing and normalization for uploaded, HTTP-polled, and WebSocket feed payloads.

This module owns:

- JSON and NDJSON feed parsing;
- tile and expert event normalization;
- contract validation before events enter UI state;
- structured rejection messages for invalid feed payloads.
