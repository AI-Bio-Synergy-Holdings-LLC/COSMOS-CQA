# Reports

Owns validation report assembly and export.

Initial extraction targets from legacy v3:

- SBOM export hooks;
- test report references;
- label export packaging;
- validation report JSON generation before any PDF generation.
