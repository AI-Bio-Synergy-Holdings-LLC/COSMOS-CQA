# Tile Synthesis

Owns deterministic synthetic tile generation used for demos, calibration, and tests.

Initial extraction targets from legacy v3:

- base noise generation;
- stripe, dipole, ringing, point, and clean artifact classes;
- seeded randomness;
- image stretch and palette-neutral tile output.

