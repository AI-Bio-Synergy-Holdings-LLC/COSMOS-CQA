# Evidence Module

Browser compatibility re-export for research session and evidence bundle helpers from `packages/core`.

UI-specific session download, upload, and state-restore wiring lives in `apps/web/src/ui/`.
