# Controlled Restart Override Remediation

Date: 2026-08-14

Repository: `AI-Bio-Synergy-Holdings-LLC/COSMOS-CQA`

Delivery pull request: `#93`

Delivery merge commit: `21b76f208809670c5952a6d709cd03fada2bba81`

Status: `PENDING_NORMAL_PROTECTED_PROOF_MERGE`

## Decision

The deterministic evidence-bundle receipt delivery is operationally green,
but it cannot receive a clean charter closeout. GitHub recorded an immutable
`protected_branch.policy_override` event at
`2026-08-14T04:30:18.507Z`, directly against the protected squash merge of
pull request `#93`.

This record does not erase, relabel, or treat that event as a normal protected
merge. COSMOS-CQA remains one of exactly four engineering-active repositories,
and admission of a fifth repository remains prohibited until the Control Plane
and central ledger complete a conditional closeout.

## Exact override evidence

The organization audit entry is document
`c6VU5TOvEQp-1JpwstGa3g`. It records:

- before: `fc9d8d51d4d203eef136920d77da6c10a3e00f33`;
- after: `21b76f208809670c5952a6d709cd03fada2bba81`;
- branch: `refs/heads/main`;
- overridden code: `required_status_checks`; and
- reason: required status check `Analyze (actions)` was expected.

The same exact pull-request head,
`84781812fa9acb70bc924baee1387639ea139803`, had a successful
`Analyze (actions)` check from GitHub Actions app `15368`:

- workflow run: `31769007910`;
- job: `94670797050`;
- conclusion: `success`; and
- CodeQL analysis: `1617471659`, category `/language:actions`, with zero
  results.

The evidence therefore supports a required-check evaluation inconsistency. It
does not establish the vendor-internal cause of that inconsistency.

## Enforcement correction

The correction was applied without an enforcement gap:

1. At `2026-08-14T04:40:25.613Z`, repository ruleset `20831277`,
   `CodeQL semantic merge protection`, was activated against the default
   branch.
2. The ruleset requires the `CodeQL` tool, blocks high-or-higher security
   alerts and error-level alerts, blocks while analysis is in progress, and
   blocks when the required tool is not configured.
3. The ruleset has no bypass actors, and the current user cannot bypass it.
4. At `2026-08-14T04:40:35Z`, only the brittle job-name requirements
   `Analyze (actions)` and `Analyze (javascript-typescript)` were removed from
   classic branch protection.
5. Classic protection remains strict and app-pinned to GitHub Actions app
   `15368` for `Web checks`, `policy / Organization baseline`, and
   `metadata / PR metadata`.

This replaces job-name coupling with GitHub's semantic code-scanning merge
protection. It does not disable default CodeQL setup or remove Actions and
JavaScript/TypeScript analysis.

## Delivery health

The exact delivery merge commit completed successfully:

- CI: run `31770071558`;
- public deployment: run `31770071378`;
- organization policy: run `31770071365`;
- CodeQL: runs `31770070849` and `31770071256`; and
- open high/critical Dependabot, CodeQL, and secret-scanning alerts: zero.

The checksum receipt remains limited to deterministic byte consistency. It
does not establish authorship, authenticity, scientific validity, production
readiness, regulatory suitability, or certification.

## Conditional closeout gate

This remediation is accepted only when all of the following are true:

1. the pull request containing this record reaches `main` through the normal
   protected merge path;
2. semantic CodeQL merge protection and all three classic required checks are
   successful at its exact head;
3. no new `protected_branch.policy_override` event is generated by that proof
   merge;
4. exact-main CI, policy, CodeQL, and public deployment are successful;
5. the existing Control Plane records a signed deviation review; and
6. the central organization ledger records the conditional closeout through a
   separate protected pull request.

Until all six conditions are met, the COSMOS-CQA bounded delivery is not
closed and no fifth repository may enter engineering.

## Authority boundary

This remediation changes merge-control representation only. It creates no
backend, provider, authentication, private-application, Holdings connector,
execution, actuation, release-authority, or Gate 6 path.

The machine-readable evidence record is
`docs/evidence/controlled-restart-override-remediation-2026-08-14.json`.
